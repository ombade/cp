#include <stdio.h>
int main()
{
    int size, i;
    printf("Enter the size of the stack");
    scanf("%d", &size);
    for (i = 0; i < size; i++)
    {
        printf("The matrix function is ");
    }
}